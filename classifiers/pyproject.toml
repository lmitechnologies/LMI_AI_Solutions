[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "classifiers"
version = "1.6.0"
description = "LMI AI Solutions classifiers"


[tool.setuptools]
packages = ["yolov8_cls", "cls_core"]