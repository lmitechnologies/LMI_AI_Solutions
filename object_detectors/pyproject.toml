[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "object_detectors"
version = "2.3.0.0"
description = "LMI AI Solutions object detectors"

[tool.setuptools]
packages = ["ultralytics_lmi", "yolov8_lmi", "od_core", "detectron2_lmi", "yolov5_lmi"]