version: "3.9"
services:
  ais_test:
    build:
      context: .
      dockerfile: ./dockerfile
    volumes:
      - ../:/app/repo
    runtime: nvidia # ensure that Nvidia Container Toolkit is installed
    ipc: host
    command: >
      bash -c "cd /app/repo && pytest"
