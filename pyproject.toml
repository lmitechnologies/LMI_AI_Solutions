[tool.ruff]
line-length = 90
exclude = [
    "submodules",
    "assets",
    "__pycache__",
    "build",
    "dist",
]

[tool.pytest.ini_options]
log_cli = true
log_level = "INFO"
testpaths = ["tests"]
norecursedirs = ["submodules", "assets", "__pycache__"]
