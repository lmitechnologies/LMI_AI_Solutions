name: Build and Publish Test Docker Image

on:
  workflow_dispatch:

jobs:
  build-and-push-test-env:
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        python-version: ['3.10']
        runner-config:
          - runner: ubuntu-latest
            platform: linux/amd64
            arch: amd64
          - runner: ubuntu-24.04-arm
            platform: linux/arm64
            arch: arm64

    runs-on: ${{ matrix.runner-config.runner }}

    env:
      IMAGE_NAME: ghcr.io/lmitechnologies/ais-test-env

    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=py${{ matrix.python-version }}-${{ matrix.runner-config.arch }}

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./tests/dockerfile.ci
          platforms: ${{ matrix.runner-config.platform }}
          build-args: |
            PYTHON_VERSION=${{ matrix.python-version }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Clean up old untagged images
        if: success()
        uses: actions/delete-package-versions@v5
        with:
          package-name: 'ais-test-env'
          package-type: 'container'
          min-versions-to-keep: 3
          delete-only-untagged-versions: true