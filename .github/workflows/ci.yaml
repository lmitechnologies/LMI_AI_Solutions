name: CI

on:
  push:
    branches: [ "ais" ]
  pull_request:
    branches: [ "ais" ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check-changes:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      skip_tests: ${{ steps.check.outputs.skip_tests }}
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: false
          submodules: false
          fetch-depth: 0
          
      - name: Check if only docs changed
        id: check
        run: |
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            git fetch origin ${{ github.base_ref }}
            base="origin/${{ github.base_ref }}"
          else
            base="${{ github.event.before }}"
            if [ -z "$base" ] || [ "$base" == "0000000000000000000000000000000000000000" ]; then
              base="HEAD~1"
            fi
          fi
          
          echo "Comparing against: $base"
          files=$(git diff --name-only $base HEAD)
          
          echo "Changed files:"
          echo "$files"
          
          code_files=$(echo "$files" | grep -vE '(^docs/|\.md$|^\.gitignore$|^\.gitattributes$)' || true)
          
          if [ -z "$code_files" ]; then
            echo "skip_tests=true" >> $GITHUB_OUTPUT
          else
            echo "skip_tests=false" >> $GITHUB_OUTPUT
          fi

  run-test:
    needs: check-changes
    if: ${{ needs.check-changes.outputs.skip_tests != 'true' && github.head_ref != 'update-pre-commit-hooks' }}
    
    strategy:
      fail-fast: false
      matrix:
        runner-config:
          - runner: ubuntu-latest
            arch: amd64
          - runner: ubuntu-24.04-arm
            arch: arm64
        python-version: ['3.10']
        test_scenario: 
          - name: "no_ad"
            version: "v1.1.1"
          - name: "ad_v1"
            version: "v1.1.1"
          - name: "ad_v2"
            version: "v2.2.0" 
    
    timeout-minutes: 30
    runs-on: ${{ matrix.runner-config.runner }}
    
    # Dynamically select the image based on the matrix version
    container:
      image: ghcr.io/lmitechnologies/ais-test-env:py${{ matrix.python-version }}-${{ matrix.runner-config.arch }}-${{ matrix.test_scenario.version }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          lfs: false
          submodules: 'true'

      - name: Mark workspace as safe
        run: git config --global --add safe.directory $(pwd)

      - name: Create Git LFS file list
        run: git lfs ls-files -l |cut -d' ' -f1 |sort >.git/lfs-hashes.txt

      - name: Restore Git LFS object cache
        uses: actions/cache@v4
        with:
          path: .git/lfs
          key: lfsdata-v1-${{ hashFiles('.git/lfs-hashes.txt') }}
          restore-keys: |
            lfsdata-v1-
            lfsdata

      - name: Fetch any needed Git LFS objects and prune extraneous ones
        run: git lfs fetch --prune

      - name: Check out Git LFS content
        run: git lfs checkout

      - name: Install LMI AI Solutions packages
        run: |
          pip install -e .
          
      - name: Run tests
        shell: bash
        run: |
          MODE="${{ matrix.test_scenario.name }}"
          echo "Running Test Scenario: $MODE"
          
          # Check if the mode string starts with 'ad_'
          if [[ "$MODE" == ad_v1 ]]; then
            echo "Running Anomaly Detection Tests..."
            pytest tests/anomaly_detectors/anomalib_lmi/test_anomaly_model2.py
          elif [[ "$MODE" == ad_v2 ]]; then
            echo "Running Anomaly Detection Tests with Anomalib v2.2.0..."
            pytest tests/anomaly_detectors/anomalib_lmi/test_anomaly_model_v2.py
          else
            echo "Running Standard (No-AD) Tests..."
            OTHER_TEST_SUITE=(lmi_utils object_detectors classifiers)
            for suite in "${OTHER_TEST_SUITE[@]}"; do
              pytest tests/${suite}
            done
          fi

  ci-success:
    runs-on: ubuntu-latest
    needs: run-test
    if: always()
    steps:
      - name: Check run-test matrix status
        run: |
          result="${{ needs.run-test.result }}"
          
          echo "Overall 'run-test' result: $result"
          
          if [[ $result == "success" || $result == "skipped" ]]; then
            echo "CI check passed."
            exit 0
          else
            echo "CI check failed. The matrix status was: $result"
            exit 1
          fi